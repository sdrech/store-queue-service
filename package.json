{
    "name": "store-queue-service",
    "version": "1.0.0",
    "description": "Store service handles all records according to Queue principle",
    "main": "dist/main.js",
    "types": "dist/main.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "start": "npm run build && node dist/main",
        "test": "jest",
        "test:watch": "jest --watchAll"
    },
    "devDependencies": {
        "@types/jest": "^29.5.13",
        "@types/node": "^18.16.0",
        "jest": "^29.7.0",
        "ts-jest": "^29.2.5",
        "ts-node": "^10.9.2",
        "typescript": "^5.6.2"
    }
}
